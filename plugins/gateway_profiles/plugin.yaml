name: gateway-profiles
version: 0.1.0
description: Per-chat gateway profiles for messaging triggers, skills, prompts, model routing, toolsets, and reply suppression.
author: felipefvc
kind: standalone
provides_hooks:
  - pre_gateway_dispatch
  - gateway_runtime_override
provides_commands:
  - profiles
